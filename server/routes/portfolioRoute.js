// const express = require("express");
// const router = express.Router();
// const { Stocks, Users } = require("../models");
// const { validateToken } = require("../middlewares/AuthMiddleware");

// router.get("/", async (req, res) => {
//   const user = await User
// });
